# Base URL of the API under test.
# Override at runtime: mvn test -Dbase.url=https://staging.example.com
base.url=https://jsonplaceholder.org

# HTTP timeouts in milliseconds
connect.timeout=10000
read.timeout=10000
